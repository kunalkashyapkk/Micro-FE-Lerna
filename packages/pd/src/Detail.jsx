import React from "react";

export default function Detail() {
    return (
        <div className="mt-10 text-3xl mx-auto max-w-6xl">
            <div className="my-10">Detail - App3
                <li>Modern JavaScript is a “safe” programming language. It does not provide low-level access to memory or the CPU, because it was initially created for browsers which do not require it.</li>
                <li>JavaScript's capabilities greatly depend on the environment it's running in. For instance, Node.js supports functions that allow JavaScript to read/write arbitrary files, perform network requests, etc.</li>
                <li>In-browser JavaScript can do everything related to webpage manipulation, interaction with the user, and the webserver.</li>
            </div>
        </div>
    );
}